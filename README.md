# CNN Classification

This project uses a Convolutional Neural Network (CNN) to classify animal images.

## Tools Used
Python, NumPy, TensorFlow, Matplotlib

  Run
Install required libraries and open `animals.ipynb` to train and test the model.

The model is trained on labeled animal data and shows prediction accuracy.
